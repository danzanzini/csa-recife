<p id="notice"><%= notice %></p>

<div class="mt-3">
  <div class="float-left h3">
    Oferendas
  </div>

  <div class='float-right'>
    <%= link_to 'Nova oferenda', new_offering_path, class: 'btn btn-primary' %>
    <%= link_to 'Voltar', root_path, class: 'btn btn-secondary' %>
  </div>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">Local de Colheita</th>
      <th scope="col">Encerra em</th>
      <th scope="col">Ações</th>
    </tr>
  </thead>

  <tbody>
    <% @offerings.each do |offering| %>
      <tr>

        <th scope="row">
          <%= link_to offering do %>
            <%= offering.location&.name %>
          <% end %>
        </th>
        <td><%= offering.closes_at.strftime('%d/%m - %A, %H:%M') %></td>

        <td>
          <%= link_to 'Visualizar', edit_offering_path(offering) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

