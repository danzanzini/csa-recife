<div class="navbar">
  <div class="h3 float-left">
    Editar Apoiador
  </div>

  <div class='float-right'>
    <div class="float-left mr-1">
      <%= button_to( status_button_label(@user.active),
        toggle_active_user_path(@user),
        class: 'btn btn-primary'
      ) %>
    </div>
    <%= link_to 'Voltar', supporters_path, class: 'btn btn-secondary' %>
  </div>
</div>

<%= render 'form', user: @user %>
